import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window")

export const W100 = width
export const H100 = height
export const W95 = width * 0.95
export const W90 = width * 0.90